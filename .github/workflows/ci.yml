name: CI
on:
  push:
    branches: [ main, work ]
  pull_request:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm run test:router
      - run: node --test cap/tests/mm.test.js cap/tests/fi.test.js cap/tests/router.test.js
